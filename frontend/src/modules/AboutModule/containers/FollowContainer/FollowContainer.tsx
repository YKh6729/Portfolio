import { FC } from "react";
import styles from "../../AboutModule.module.css";

const FollowContainer: FC = () => {
    return (
        <section className={styles.followSection}>
            Some links
        </section>
    );
};

export default FollowContainer;